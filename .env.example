# Copy to `.env` (same directory) and fill in your key. `.env` is gitignored.
# The script reads MIROMIND_API_KEY (NOT "APIKEY").
MIROMIND_API_KEY=sk_live_your_key_here
# Optional — only if your gateway differs from the default:
# MIROMIND_BASE_URL=https://api.miromind.ai/v1
